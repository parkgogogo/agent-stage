# agentstage

Monorepo (pnpm workspace) for Agent UI runtime and CLI tools.

## Packages

- `@agentstage/bridge` - Bridge state management
- `@agentstage/render` - JSON to React component renderer
- `agent-stage` - CLI for creating and managing Agent UIs
